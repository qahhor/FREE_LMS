apiVersion: v1
kind: Secret
metadata:
  name: freelms-secrets
  namespace: freelms
type: Opaque
stringData:
  # IMPORTANT: Replace these with actual secrets before deployment!
  # Use: kubectl create secret generic freelms-secrets --from-literal=...

  DB_USER: "CHANGE_ME"
  DB_PASSWORD: "CHANGE_ME"
  JWT_SECRET: "CHANGE_ME_USE_256_BIT_SECRET"
  EUREKA_USER: "CHANGE_ME"
  EUREKA_PASSWORD: "CHANGE_ME"
  REDIS_PASSWORD: "CHANGE_ME"
  STRIPE_SECRET_KEY: "CHANGE_ME"
  STRIPE_WEBHOOK_SECRET: "CHANGE_ME"
