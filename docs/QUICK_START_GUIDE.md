# 🚀 Smartup LMS — Пошаговое руководство по развертыванию

## Для кого это руководство?

Это руководство написано простым языком для:
- 👨‍💻 Начинающих DevOps-специалистов
- 🎓 Студентов и преподавателей
- 👔 Менеджеров, желающих понять процесс
- 🔧 Любого, кто хочет запустить собственную LMS

**Время на развертывание:** 30-60 минут

---

## 📋 Содержание

1. [Что такое Smartup LMS?](#1-что-такое-smartup-lms)
2. [Что нужно для запуска?](#2-что-нужно-для-запуска)
3. [Подготовка компьютера](#3-подготовка-компьютера)
4. [Скачивание проекта](#4-скачивание-проекта)
5. [Быстрый запуск (Docker)](#5-быстрый-запуск-docker)
6. [Проверка работы](#6-проверка-работы)
7. [Первоначальная настройка](#7-первоначальная-настройка)
8. [Запуск для разработки](#8-запуск-для-разработки)
9. [Частые проблемы и решения](#9-частые-проблемы-и-решения)
10. [Следующие шаги](#10-следующие-шаги)

---

## 1. Что такое Smartup LMS?

**Smartup LMS** — это система управления обучением (Learning Management System), которая позволяет:

| Возможность | Описание |
|-------------|----------|
| 📚 Создавать курсы | Видео, текст, тесты, задания |
| 👥 Управлять пользователями | Студенты, преподаватели, администраторы |
| 📊 Отслеживать прогресс | Статистика, отчёты, аналитика |
| 🎮 Геймификация | Баллы, достижения, рейтинги |
| 🏢 Мультитенантность | Несколько организаций в одной системе |

### Архитектура простыми словами

```
┌─────────────────────────────────────────────────────────────┐
│                     ВАШ БРАУЗЕР                             │
│              (Chrome, Firefox, Safari)                      │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    API GATEWAY                               │
│         (Главный вход в систему, порт 8000)                 │
└─────────────────────────┬───────────────────────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        ▼                 ▼                 ▼
┌───────────────┐ ┌───────────────┐ ┌───────────────┐
│ Сервис        │ │ Сервис        │ │ Сервис        │
│ авторизации   │ │ курсов        │ │ ... и другие  │
│ (вход/выход)  │ │ (контент)     │ │ (20 штук)     │
└───────┬───────┘ └───────┬───────┘ └───────┬───────┘
        │                 │                 │
        └─────────────────┼─────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    БАЗА ДАННЫХ                               │
│              (PostgreSQL — хранит все данные)               │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. Что нужно для запуска?

### Минимальные требования к компьютеру

| Компонент | Минимум | Рекомендуется |
|-----------|---------|---------------|
| 💻 Процессор | 2 ядра | 4+ ядра |
| 🧠 Оперативная память | 8 GB | 16 GB |
| 💾 Свободное место | 20 GB | 50 GB SSD |
| 🌐 Интернет | Стабильный | Стабильный |

### Операционная система

Поддерживаются:
- ✅ **Windows 10/11** (с WSL2)
- ✅ **macOS** (10.15+)
- ✅ **Linux** (Ubuntu 20.04+, CentOS 8+)

---

## 3. Подготовка компьютера

### Шаг 3.1: Установка Docker

**Docker** — это программа, которая позволяет запускать приложения в изолированных контейнерах. Думайте о контейнерах как о "коробках", в которых уже есть всё необходимое для работы программы.

#### 🪟 Windows

1. **Скачайте Docker Desktop:**
   - Перейдите на [docker.com/products/docker-desktop](https://www.docker.com/products/docker-desktop/)
   - Нажмите "Download for Windows"

2. **Установите:**
   - Запустите скачанный файл `Docker Desktop Installer.exe`
   - Следуйте инструкциям установщика
   - ⚠️ Когда спросят про WSL2 — согласитесь (поставьте галочку)

3. **Перезагрузите компьютер**

4. **Запустите Docker Desktop:**
   - Найдите в меню "Пуск" → "Docker Desktop"
   - Дождитесь, пока иконка в трее станет зелёной (1-2 минуты)

5. **Проверьте установку:**
   - Откройте PowerShell (правой кнопкой на "Пуск" → "Windows PowerShell")
   - Введите команду:
   ```powershell
   docker --version
   ```
   - Должно появиться что-то вроде: `Docker version 24.0.7`

#### 🍎 macOS

1. **Скачайте Docker Desktop:**
   - Перейдите на [docker.com/products/docker-desktop](https://www.docker.com/products/docker-desktop/)
   - Нажмите "Download for Mac"
   - Выберите версию для вашего процессора:
     - **Apple Silicon** (M1, M2, M3) — если Mac куплен после 2020
     - **Intel** — если Mac старше 2020 года

2. **Установите:**
   - Откройте скачанный файл `.dmg`
   - Перетащите иконку Docker в папку "Программы"

3. **Запустите:**
   - Откройте "Программы" → "Docker"
   - Разрешите доступ, если система спросит
   - Дождитесь запуска (иконка кита в верхней панели)

4. **Проверьте:**
   ```bash
   docker --version
   ```

#### 🐧 Linux (Ubuntu/Debian)

Откройте терминал (Ctrl+Alt+T) и выполните команды по очереди:

```bash
# 1. Обновляем список пакетов
sudo apt update

# 2. Устанавливаем необходимые компоненты
sudo apt install -y apt-transport-https ca-certificates curl software-properties-common

# 3. Добавляем официальный ключ Docker
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg

# 4. Добавляем репозиторий Docker
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# 5. Устанавливаем Docker
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

# 6. Добавляем текущего пользователя в группу docker (чтобы не писать sudo)
sudo usermod -aG docker $USER

# 7. ВАЖНО: Выйдите из системы и войдите снова (или перезагрузите)
```

После перезагрузки проверьте:
```bash
docker --version
docker compose version
```

---

### Шаг 3.2: Установка Git

**Git** — это система контроля версий, которая позволяет скачивать проекты с GitHub.

#### 🪟 Windows

1. Скачайте: [git-scm.com/download/win](https://git-scm.com/download/win)
2. Запустите установщик
3. Нажимайте "Next" везде (настройки по умолчанию подходят)
4. Проверьте в PowerShell:
   ```powershell
   git --version
   ```

#### 🍎 macOS

Git обычно уже установлен. Проверьте:
```bash
git --version
```

Если нет — установите через терминал:
```bash
xcode-select --install
```

#### 🐧 Linux

```bash
sudo apt install -y git
git --version
```

---

## 4. Скачивание проекта

### Шаг 4.1: Откройте терминал

- **Windows:** PowerShell или Git Bash
- **macOS:** Terminal (в папке "Программы" → "Утилиты")
- **Linux:** Terminal (Ctrl+Alt+T)

### Шаг 4.2: Выберите папку для проекта

```bash
# Переходим в домашнюю папку
cd ~

# Создаём папку для проектов (если её нет)
mkdir -p projects

# Переходим в неё
cd projects
```

### Шаг 4.3: Скачайте проект

```bash
# Клонируем репозиторий
git clone https://github.com/your-org/smartup-lms.git

# Переходим в папку проекта
cd smartup-lms
```

> 💡 **Что произошло?** Git скачал все файлы проекта с GitHub на ваш компьютер.

### Шаг 4.4: Проверьте, что всё скачалось

```bash
# Показать содержимое папки
ls -la
```

Вы должны увидеть:
```
backend-java/
docs/
frontend/
README.md
...
```

---

## 5. Быстрый запуск (Docker)

Это самый простой способ запустить проект. Docker сам скачает и настроит все компоненты.

### Шаг 5.1: Перейдите в папку backend

```bash
cd backend-java
```

### Шаг 5.2: Запустите все сервисы

```bash
# Запускаем все контейнеры в фоновом режиме
docker compose up -d
```

> ⏳ **Первый запуск займёт 5-15 минут** — Docker скачивает образы (около 2-3 GB).
>
> Вы увидите много текста — это нормально. Дождитесь окончания.

### Шаг 5.3: Проверьте статус контейнеров

```bash
docker compose ps
```

Вы должны увидеть список контейнеров со статусом `Up` или `healthy`:

```
NAME                    STATUS          PORTS
freelms-postgres        Up (healthy)    5432
freelms-redis           Up (healthy)    6379
freelms-kafka           Up              9092
freelms-gateway         Up              0.0.0.0:8000->8000
freelms-auth            Up              8081
freelms-courses         Up              8082
...
```

### Шаг 5.4: Дождитесь полного запуска

Сервисы запускаются по очереди. Подождите 2-3 минуты и проверьте:

```bash
# Проверяем логи gateway (главный сервис)
docker compose logs gateway-service | tail -20
```

Когда увидите строку `Started GatewayServiceApplication` — система готова!

---

## 6. Проверка работы

### Шаг 6.1: Откройте браузер

Перейдите по адресу:
```
http://localhost:8000/actuator/health
```

Вы должны увидеть:
```json
{"status":"UP"}
```

### Шаг 6.2: Проверьте Service Registry (Eureka)

Откройте:
```
http://localhost:8761
```

**Логин:** eureka
**Пароль:** eureka123

Вы увидите список всех запущенных микросервисов.

### Шаг 6.3: Проверьте API документацию (Swagger)

Откройте:
```
http://localhost:8000/swagger-ui.html
```

Здесь можно посмотреть и протестировать все API endpoints.

---

## 7. Первоначальная настройка

### Шаг 7.1: Создайте первого администратора

Используя Swagger UI или curl:

```bash
curl -X POST http://localhost:8000/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "password": "Admin123!",
    "firstName": "Администратор",
    "lastName": "Системы",
    "role": "ADMIN"
  }'
```

### Шаг 7.2: Войдите в систему

```bash
curl -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "password": "Admin123!"
  }'
```

В ответе вы получите токен доступа (accessToken), который нужен для работы с API.

---

## 8. Запуск для разработки

Если вы хотите изменять код, следуйте этим шагам.

### Шаг 8.1: Установите Java 21

#### 🪟 Windows

1. Скачайте: [adoptium.net](https://adoptium.net/) → Temurin 21 → Windows x64
2. Запустите установщик
3. Проверьте:
   ```powershell
   java -version
   ```

#### 🍎 macOS

```bash
# Установка через Homebrew
brew install openjdk@21

# Добавление в PATH
echo 'export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc

# Проверка
java -version
```

#### 🐧 Linux

```bash
sudo apt install -y openjdk-21-jdk
java -version
```

### Шаг 8.2: Установите Maven

#### 🪟 Windows

1. Скачайте: [maven.apache.org/download.cgi](https://maven.apache.org/download.cgi)
2. Распакуйте в `C:\Program Files\Maven`
3. Добавьте `C:\Program Files\Maven\bin` в переменную PATH
4. Проверьте:
   ```powershell
   mvn -version
   ```

#### 🍎 macOS

```bash
brew install maven
mvn -version
```

#### 🐧 Linux

```bash
sudo apt install -y maven
mvn -version
```

### Шаг 8.3: Запустите только инфраструктуру в Docker

```bash
cd backend-java

# Запускаем только базы данных и Kafka
docker compose up -d postgres redis kafka zookeeper minio
```

### Шаг 8.4: Соберите проект

```bash
# Сборка всех модулей (без тестов для скорости)
mvn clean package -DskipTests
```

> ⏳ Первая сборка займёт 5-10 минут — Maven скачивает зависимости.

### Шаг 8.5: Запустите сервисы по очереди

Откройте несколько терминалов:

**Терминал 1 — Service Registry:**
```bash
cd services/service-registry
mvn spring-boot:run
```
> Дождитесь: `Started ServiceRegistryApplication`

**Терминал 2 — Config Server:**
```bash
cd services/config-server
mvn spring-boot:run
```
> Дождитесь: `Started ConfigServerApplication`

**Терминал 3 — Gateway:**
```bash
cd services/gateway-service
mvn spring-boot:run
```

**Терминал 4+ — Остальные сервисы:**
```bash
cd services/auth-service
mvn spring-boot:run
```

---

## 9. Частые проблемы и решения

### ❌ Порт уже занят

**Ошибка:** `Port 8000 is already in use`

**Решение:**
```bash
# Найти процесс, занимающий порт
# Windows:
netstat -ano | findstr :8000

# macOS/Linux:
lsof -i :8000

# Остановить процесс (замените PID на номер из предыдущей команды)
# Windows:
taskkill /PID <PID> /F

# macOS/Linux:
kill -9 <PID>
```

---

### ❌ Docker не запускается

**Ошибка:** `Cannot connect to the Docker daemon`

**Решение:**
- **Windows/macOS:** Убедитесь, что Docker Desktop запущен (иконка в трее)
- **Linux:**
  ```bash
  sudo systemctl start docker
  sudo systemctl enable docker
  ```

---

### ❌ Не хватает памяти

**Ошибка:** `Container killed due to memory limit`

**Решение:**

1. **Docker Desktop (Windows/macOS):**
   - Откройте Docker Desktop → Settings → Resources
   - Увеличьте Memory до 8 GB минимум
   - Нажмите "Apply & Restart"

2. **Linux:**
   ```bash
   # Проверить доступную память
   free -h

   # Увеличить swap (если нужно)
   sudo fallocate -l 4G /swapfile
   sudo chmod 600 /swapfile
   sudo mkswap /swapfile
   sudo swapon /swapfile
   ```

---

### ❌ Ошибка при сборке Maven

**Ошибка:** `Could not resolve dependencies`

**Решение:**
```bash
# Очистить кэш Maven
rm -rf ~/.m2/repository

# Пересобрать с принудительным обновлением
mvn clean install -DskipTests -U
```

---

### ❌ Сервис не регистрируется в Eureka

**Ошибка:** Сервис не появляется в http://localhost:8761

**Решение:**
1. Проверьте, что Service Registry запущен первым
2. Подождите 30-60 секунд (регистрация не мгновенная)
3. Проверьте логи сервиса:
   ```bash
   docker compose logs auth-service | tail -50
   ```

---

### ❌ Ошибка подключения к базе данных

**Ошибка:** `Connection refused to postgres:5432`

**Решение:**
```bash
# Проверить статус PostgreSQL
docker compose ps postgres

# Если статус не "healthy", перезапустить
docker compose restart postgres

# Проверить логи
docker compose logs postgres
```

---

## 10. Следующие шаги

### 📚 Изучите документацию

| Документ | Описание |
|----------|----------|
| [README.md](../README.md) | Общее описание проекта |
| [DEPLOYMENT.md](../backend-java/DEPLOYMENT.md) | Production развертывание |
| [API Docs](http://localhost:8000/swagger-ui.html) | Документация API |

### 🔧 Настройте под себя

1. **Измените пароли** в `backend-java/.env`
2. **Настройте email** для уведомлений
3. **Добавьте SSL сертификат** для HTTPS
4. **Настройте backup** базы данных

### 🚀 Разверните в production

Для production рекомендуем:
- Использовать Kubernetes или Docker Swarm
- Настроить мониторинг (Prometheus + Grafana)
- Настроить CI/CD (GitHub Actions)
- Использовать управляемые базы данных (AWS RDS, GCP Cloud SQL)

---

## 📞 Нужна помощь?

- **Документация:** Папка `docs/` в проекте
- **Issues:** Создайте issue на GitHub
- **Email:** support@smartup.uz

---

## 🎉 Поздравляем!

Если вы дочитали до этого момента и успешно запустили проект — вы молодец!

Теперь у вас есть собственная система управления обучением, готовая к использованию.

---

<div align="center">

**Smartup LMS** — Обучение должно быть доступным каждому 📚

</div>
